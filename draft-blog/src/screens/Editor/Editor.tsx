import React from "react";

import Styles from "./Editor.module.scss";

const Editor = () => {
  return (
    <div className={Styles.container}>
      <div className={Styles.containerInner}>Editor</div>
    </div>
  );
};

export default Editor;
